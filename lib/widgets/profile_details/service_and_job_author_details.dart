import 'package:flutter/material.dart';

class ServiceAndJobAuthorDetails extends StatelessWidget {
  const ServiceAndJobAuthorDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
